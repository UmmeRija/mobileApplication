void main(){
  const total = 500;
  print(total);
  const math = 90;
  const english = 85;
  const physics = 96;
  const chemistry = 80;
  const urdu = 82;

  const obtainedMarks = math + english + physics + chemistry + urdu;
  print(obtainedMarks);

  const result = (obtainedMarks / total)*100;
  print(result);

}